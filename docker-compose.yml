services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network

  backend:
    build: ./LDJS-Back-end-Automatisez-les-tests-et-le-release-continus-avec-Docker
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongodb:27017/workshopsdb
      - CORS_ORIGIN=http://localhost
    depends_on:
      - mongodb
    networks:
      - app-network

  frontend:
    build: ./LDJS-Front-end-Automatisez-les-tests-et-le-release-continus-avec-Docker
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  mongo-data: